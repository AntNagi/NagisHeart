# Start long-screen authority note

当前目录只保留 `rethink/` 作为 Start v23 长屏适配的有效策略记录。

已清理到 archive 的候选：

- V1：`start_long/base/`、`start_long/layers/`、`start_long/previews/` 与 V1 spec / manifest / self-check
- V2：`start_long/v2/`
- V3：`start_long/v3/`

原因：

- V1 使用上下毛玻璃 / 模糊补边，已被 Ant大小姐打回。
- V2 / V3 走重新裁成近景大头方向，未保留用户要求的人物关系，也已被打回。
- 当前有效方向是 `rethink/` 中的 Strategy A：保留 Start v23 / remeet 背景方向，使用布局适配保护标题、START 与人物关系。

归档位置：

`00_harness/99_archive/obsolete_assets/20260718_cleanup/design/authority/icon_start_tt/start_long/`

后续开发不得接入已归档的 V1 / V2 / V3 资源包。

