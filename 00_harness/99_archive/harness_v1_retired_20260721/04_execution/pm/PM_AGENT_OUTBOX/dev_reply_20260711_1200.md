# Dev Reply — 2026-07-11 12:00

## Issue Status

### AND-001 — 默认名确认
**Status**: RESOLVED
`NameSetupScreen.kt` line 27: `var name by remember { mutableStateOf("Ant") }` — default name is "Ant".

### AND-002 — Prologue 路由
**Status**: RESOLVED
NavGraph routes: NameSetup → Prologue → Game. Flow verified in `NavGraph.kt`.

### AND-003 — {{playerName}} 替换
**Status**: RESOLVED
Fixed in `GameViewModel.kt`:
- `showDialogueLine()` — extracted `resolvedSpeaker` variable, used for both backlog and uiState
- `showResponseLine()` — same fix applied
Both speaker name and dialogue text now resolve `{{playerName}}` before display and backlog storage.

### AND-004 — debug label 外露
**Status**: RESOLVED
- `buildDebugInfo()` already returns `null` when `!BuildConfig.DEBUG_MODE` (release builds never show it)
- NEW: Debug overlay is now **hidden by default** even in debug builds. Toggle via **long-press on HUD chapter title**. QA testers won't see it unless deliberately activated.
- Files changed: `GameScreen.kt` (added `debugOverlayVisible` state gate), `NagiHud.kt` (added `onTitleLongPress` callback with `combinedClickable`)

### AND-005 — 输入布局
**Status**: RESOLVED
Added `imePadding()` to `NameSetupScreen.kt` Column modifier. When the soft keyboard opens, the content shifts up so the input field and confirm button remain visible.

## Summary
All 5 issues resolved. No blockers. Ready for Laud retest.
